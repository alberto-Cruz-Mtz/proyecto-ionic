import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bearhug-management-ionic-angular',
  webDir: 'www'
};

export default config;
